import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.themealdb.com", "www.thecocktaildb.com", "img.freepik.com"], // adicione outros domínios se necessário
  },
};

export default nextConfig;
